apiVersion: 1
providers:
  - name: 'WikipediaAnalytics'
    folder: ${GRAFANA_INSTANCE_DASHBOARDS_GROUP}
    folderUid: 'wikipedia'
    type: file
    updateIntervalSeconds: 10
    disableDeletion: true
    allowUiUpdates: true
    options:
      path: ${GRAFANA_INSTANCE_DASHBOARDS_DIR}
